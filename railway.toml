[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 4 --timeout 180 --max-requests 300 --max-requests-jitter 30"
restartPolicyType = "on_failure"
healthcheckPath = "/healthz"
